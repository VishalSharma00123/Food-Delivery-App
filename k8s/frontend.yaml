apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: food-delivery/frontend:local
          imagePullPolicy: IfNotPresent
          env:
            # Tell Next.js where the API Gateway is (in a cluster or via nodeport locally)
            - name: NEXT_PUBLIC_API_URL
              value: "http://localhost:30080"
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
